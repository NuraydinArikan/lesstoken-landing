# Runtime dependencies for the Flask API image.
# Dev/test tools (pytest, black, flake8) are deliberately absent — they are not
# needed at runtime and every extra pin is another chance to break the build.
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.5
SQLAlchemy==2.0.20
PyJWT==2.8.0
requests==2.31.0
packaging==23.1
python-dotenv==1.0.0
gunicorn==21.2.0
Flask-Migrate==4.1.0

# PostgreSQL driver, used when DATABASE_URL points at Railway Postgres.
psycopg2-binary==2.9.9
